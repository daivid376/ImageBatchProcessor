{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run ImageBatchProcessor",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/src/ImageBatchProcessor_main.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "justMyCode": true
        }
    ]
}